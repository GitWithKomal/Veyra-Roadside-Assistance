apiVersion: v1
kind: Service
metadata:
  name: veyra-frontend-service
spec:
  type: NodePort
  selector:
    app: veyra-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30300